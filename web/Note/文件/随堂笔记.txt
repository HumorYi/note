1、P标签里面不能嵌套块级元素，也不能嵌套它自己本身
  原因：浏览器解析就这样
2、p、h1-h6可以直接包含行内元素和文本信息，但是不允许包含块状元素
3、dl元素只允许包含dt和dd，同时dt不能包含块状元素，只允许包含行内元素，对于dd可以包含任何元素
4、a p h1 这些都不可以包含ul（私人总结）
5、文本是采用环绕方式显示的，浮动的元素不能遮住文本，文本只会向后兼容显示
6、line-height样式设置的行高只针对文字，对图片无效
