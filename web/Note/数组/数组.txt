程序来源于生活，思维要转变，尽量用生活中的例子来解析，便于理解

数组：
	数组是引用类型的对象，能找到就叫引用

	按值传递：两个变量间赋值时，或者将变量作为参数传入函数时，其实仅是将原变量中的值复制一个副本给对方
	原始类型的值：修改新变量，不影响原始的值
	

	三个关键字不能放在简写里面，必须独自占据一行
		return,break,continue

	for语句的简写方式
		for (var i=0, max=arr[0]; i<arr.length; arr[i]>max && (max=arr[i]), i++);
	
	逻辑表达式 亦可视为 if语句的简写方式
		fromi===undefined && (fromi=0);

	concat:打散数组
	学API：干什么用的，传什么参数，返回什么？

	查API自己做思维导图，搜索关键字

	排序功能必须得做，用户体验

	一切算法默认为升序排序